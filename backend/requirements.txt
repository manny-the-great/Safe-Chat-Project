# Core Django — pinned to 3.2.x (last version compatible with Djongo)
Django>=3.2,<4.0
djangorestframework>=3.14,<4.0
djangorestframework-simplejwt>=5.3,<6.0
django-cors-headers>=3.13,<5.0

# MongoDB via Djongo — requires sqlparse==0.2.4 (pinned explicitly to avoid conflict)
djongo==1.3.6
pymongo[srv]==3.12.3
sqlparse==0.2.4
dnspython>=2.3.0

# Environment & utilities
python-dotenv>=1.0
requests>=2.31
Pillow>=10.4.0

# WebSockets (Django Channels + Daphne ASGI server)
channels>=3.0,<4.0
channels-redis>=3.4,<4.0
daphne>=3.0,<4.0

# Rate limiting
django-ratelimit>=3.0,<4.0
