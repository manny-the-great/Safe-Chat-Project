# SafeChat ML Training Dependencies
# Install these ONLY for training — NOT needed in the runtime Docker container
#
# Usage:
#   python -m pip install -r requirements_train.txt
#
# NOTE: The runtime ml_service/ uses requirements.txt (lean version).
#       This file adds the heavier training-only tools.

# ── Core Training Stack ────────────────────────────────────
torch
transformers
tokenizers

# ── Dataset & Data Processing ─────────────────────────────
pandas
numpy
scikit-learn     # AUC-ROC, F1, confusion matrix

# ── Kaggle API (auto-download dataset) ────────────────────
kaggle

# ── Progress & Utilities ──────────────────────────────────
tqdm
